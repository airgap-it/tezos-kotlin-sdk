package it.airgap.tezos.michelson.internal.utils

//internal fun <T : MichelineTransformableBuilder> T.mapNodeToSequence(): T = mapNode { if (it is MichelineSequence) it else MichelineSequence(listOf(it)) }